layer:
  id: "railway"
  description: Rail
  buffer_size: 4
  datasource:
    geometry_field: geometry
    query: (SELECT * FROM layer_railway(!bbox!, z(!scale_denominator!))) AS t
schema:
  - ./railway.sql
datasources:
  - type: imposm3
    mapping_file: ./mapping.yaml
